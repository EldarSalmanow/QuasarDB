PERSON 1 - Eldar S.:

[INFRA-1] Настройка базового репозитория и CMake

Описание: Создать репозиторий GitHub. Настроить CMakeLists.txt с разделением на таргеты: core (общая библиотека), client (терминал), server (узел-обработчик). Подключить менеджер пакетов (vcpkg/Conan) и добавить библиотеки nlohmann/json, GTest и т.д.

Критерии приемки:

Проект клонируется и собирается одной командой cmake --build .

Собираются три пустых main.cpp (для клиента и сервера).

Настроен .gitignore.

Написан базовый README.md с описанием и инструкциями по сборке и запуску.

[INFRA-2] Разработка контрактов для клиент-серверного взаимодействия

Описание: Описать в файле API.md виды запросов и ответов. Действие `query` принимает строку сырого SQL в `data.query` и возвращает статус, сообщение об ошибке и JSON-данные для SELECT.

Критерии приемки:

Написан API.md файл.

[INFRA-3] Проектирование базовых C++ интерфейсов

Описание: Написать абстрактные классы в папке core/include. IStorageEngine, IParser. Написать для них простейшие классы-заглушки, чтобы клиент и сервер могли начать общаться фейковыми данными.

Критерии приемки:

Интерфейсы залиты в main.

Сервер отвечает фейковым JSON-ом на любой запрос клиента.

[INFRA-4] Настройка Developer Experience и статического анализа

Описание: Создать devcontainer, чтобы унифицировать окружение команды. Настроить линтеры и форматтеры для автоматического контроля качества кода.

Критерии приемки:

В репозитории есть папка .devcontainer с Dockerfile и devcontainer.json.

Проект открывается в Docker-контейнере через VS Code со всеми установленными C++ extensions.

В корне есть .clang-format.

В CMake настроен запуск clang-tidy.

Добавлены опции CMake для сборки проекта с флагом -fsanitize=address.

[CLUSTER-1] Клиент-сервер, маршрутизатор запросов и шардирование (ТЗ: 3, 4)

Описание: Реализовать логику распределения данных (шардирование) на сервере Entrypoint. Балансировщик должен принимать запрос, определять таблицу и на основе хеша имени таблицы (или первичного ключа) перенаправлять запрос на соответствующий узел Storage.

Критерии приемки:

Запросы к разным таблицам распределяются между разными Storage-узлами равномерно.

Реализован механизм динамического добавления/удаления Storage-узлов "на лету" (без остановки Entrypoint).

[CLUSTER-2] Система мониторинга доступности Healthcheck / Heartbeat (ТЗ: 5)

Описание: Реализовать фоновый поток на Entrypoint, который раз в N секунд отправляет Ping на все зарегистрированные Storage узлы.

Критерии приемки:

При отсутствии ответа от узла 3 раза подряд он помечается как "мертвый".

Entrypoint принудительно перезапускает мертвый сервер.

Запросы к упавшему узлу ставятся в ожидание или возвращают ошибку, пока он не поднимется.

[ASYNC-1] Очередь задач и маршрутизация на Entrypoint (ТЗ: 6 - Часть 1)

Описание: Если запрос классифицируется как долгий, Entrypoint не ждет ответа от Storage. Он генерирует GUID v4, ставит задачу во внутреннюю очередь (thread-safe queue) и моментально отвечает клиенту. Пул потоков (Workers) разбирает очередь и шлет задачи на Storage.

Критерии приемки:

Реализована потокобезопасная очередь задач на C++.

Сервер моментально возвращает GUID клиенту.

PERSON 2 - Kirill P.

[STORAGE-1] Разработка менеджера страниц и файлового ввода-вывода

Описание: Написать модуль, который абстрагирует работу с жестким диском. Чтение и запись данных должны происходить блоками (страницами, например по 4 КБ) в бинарный файл. Потеря данных при выходе из программы недопустима.

Критерии приемки:

Можно сохранить бинарный массив байт по определенному offset в файл и корректно прочитать его обратно после перезапуска программы.

[STORAGE-2] Управление иерархией БД

Описание: Реализовать логику хранения иерархии: Уровень систем -> Уровень баз данных -> Уровень таблиц. Метаданные (какие таблицы существуют, какие у них типы данных: int / string, есть ли NOT_NULL) должны сохраняться на диск.

Критерии приемки:

Команды поддержки иерархии успешно создают или удаляют структуры на диске.

[STORAGE-3] Реализация Индекса

Описание: Реализовать структуру дерева. Важное требование ТЗ: Дублирование самих данных записей запрещено. Индекс должен хранить только ключи и ссылки (указатели/смещения) на реальные записи в файле данных. Индекс автоматически строится для колонок с модификатором INDEXED.

Критерии приемки:

Поиск по индексу работает за O(logN).

Вставка/удаление перестраивает дерево (балансировка, сплит узлов).

Индекс сериализуется и десериализуется из файловой системы.

[STORAGE-4] Выполнение запросов и Оптимизация

Описание: Реализовать вставку данных в таблицы с проверкой типа int/string и NOT_NULL (инициализация NULL если не передан). Реализовать выполнение SELECT. Если в блоке WHERE используется поле с INDEXED, поиск обязан использовать индекс, а не полное сканирование.

Критерии приемки:

Выборка возвращает корректные объекты.

При попытке вставить пустую строку в NOT_NULL возвращается семантическая ошибка.

[STORAGE-5] Подсистема String Interning / Дедупликация (ТЗ: 2)

Описание: Написать глобальный/побазовый пул уникальных строк. Все строковые значения, приходящие в INSERT или UPDATE, должны прогоняться через этот пул.

Критерии приемки:

Уникальные строки хранятся в оперативной памяти в единственном экземпляре.

В таблицах и B-деревьях вместо самих строк хранятся ссылки/указатели на них (существенная экономия памяти).

[STORAGE-6] Журнал транзакций

Описание: Для реализации Задания 1 без полных бэкапов необходим журнал отмены. Перед любым изменением данных (создание, удаление, модификация) в бинарный файл undo.log записывается обратная операция с точным Timestamp.

Критерии приемки:

Лог пишется на диск синхронно или пакетно (до подтверждения успешности запроса).

Потеря данных в логе недопустима.

[STORAGE-7] Механизм темпорального отката (ТЗ: 1)

Описание: Реализовать команду REVERT [table_name] [yyyy.mm.dd-hh:mm:ss.msmsms];. Система должна прочитать Undo-Log с конца до указанной отметки времени и применить обратные операции.

Критерии приемки:

После команды REVERT таблица возвращается в состояние на указанную миллисекунду.

Откат происходит без полного копирования файлов БД (снапшотов).

PERSON 3 - Misha S.

[CLIENT-1] Реализация подсистемы ввода

Описание: Написать IInputStream с двумя реализациями. Интерактивный режим (чтение из std::cin с приглашением qdb>) и Пакетный режим (чтение из файла, переданного как аргумент ./qdb-client script.txt).

Критерии приемки:

Поддерживается многострочный ввод (команда не отправляется, пока не введен символ ;).

Файл с набором SQL-команд корректно считывается и парсится поблочно.

[CLIENT-2] Сетевое взаимодействие и JSON-форматтер

Описание: Использовать клиент для отправки прочитанных запросов на сервер. Полученный от сервера JSON-ответ парсить с помощью nlohmann/json и красиво выводить в консоль.

Критерии приемки:

Результат SELECT выводится в виде массива JSON-объектов.

В случае ошибки возвращается информативное сообщение, текст ошибки выводится красным цветом.

[SEC-1] Хранилище аккаунтов и Аутентификация (ТЗ: 9 - Часть 1)

Описание: Реализовать систему логинов и паролей на Entrypoint. Пароли запрещено хранить в открытом виде — использовать хеширование с солью (bcrypt/SHA-256).

Критерии приемки:

Успешный логин возвращает подписанный JWT-токен.

Все последующие запросы к СУБД отклоняются (401 Unauthorized), если нет валидного токена.

[SEC-2] Подсистема разграничения доступа RBAC (ТЗ: 9 - Часть 2)

Описание: Реализовать проверку прав на базы/таблицы для пользователей и групп. Поддержка прав на чтение, запись, создание, удаление.

Критерии приемки:

Пользователь может выполнить действие, только если имеет личное разрешение, либо состоит в разрешенной группе, либо есть доступ по умолчанию для БД.

[OBS-1] Подсистема логирования активности / Access Logs (ТЗ: 7)

Описание: Внедрить запись каждого запроса в лог-файл на сервере.

Критерии приемки:

Лог содержит: тело запроса, ID клиента (из токена), ID обработчика (потока/Storage), время начала, время завершения и статус/код возврата.

Запись происходит асинхронно, не блокируя выполнение самого запроса.

[OBS-2] Децентрализованная телеметрия (ТЗ: 8)

Описание: Реализовать сбор и расчет метрик: текущий RPS (запросов в секунду), средний/макс RPS за 10 мин, среднее время обработки (за 10 сек) и Error Rate (ошибок в минуту).

Критерии приемки:

Телеметрия вычисляется без жестких блокировок (через std::atomic).

Метрики отображаются на сервере в реальном времени.

[ASYNC-2] API получения статуса и доработка Клиента (ТЗ: 6 - Часть 2)

Описание: На сервере добавить метод для проверки статуса по GUID (Pending, Running, Completed, Failed). Клиентское приложение (терминал) должно автоматически поллить (опрашивать) сервер, если получило GUID, показывая прогресс-бар пользователю.

Критерии приемки:

СУБД корректно возвращает результат или текст ошибки после завершения асинхронной задачи.

Клиент "замораживается" с красивым визуальным ожиданием, пока сервер крутит задачу в фоне.

PERSON 4 - Kirill B.

[PARSER-1] Лексер

Описание: Написать лексер, который разбивает входящую строку на токены (Ключевые слова, Идентификаторы, Строковые литералы в кавычках, Числа, Операторы).

Критерии приемки:

Лексер игнорирует лишние пробелы и переносы строк.

Ключевые слова регистронезависимы.

Покрыто Unit-тестами (GTest).

[PARSER-2] Грамматика языка запросов

Описание: Написать файл GRAMMAR.md с описанием грамматики языка запросов.

Критерии приемки:

Есть файл GRAMMAR.md с описанием грамматики языка запросов.

[PARSER-3] Парсер

Описание: Реализовать AST и его построение с помощью парсера для грамматических конструкций, описанных в GRAMMAR.md.

Критерии приемки:

Генерируются соответствующие узлы AST.

Покрыто Unit-тестами.

[SQL-1] Поддержка значений по умолчанию (ТЗ: 10)

Описание: Расширить синтаксис CREATE TABLE модификатором DEFAULT [value]. Доработать семантический анализатор.

Критерии приемки:

Если при INSERT значение для столбца с DEFAULT не передано, автоматически подставляется заданное значение.

Парсер корректно распознает новый синтаксис.

[SQL-2] Составные логические выражения (ТЗ: 11)

Описание: Расширить AST-дерево и Lexer для поддержки операторов AND, OR и группировки с помощью круглых скобок ( ). Доработать ConditionEvaluator (Visitor) для вычисления логики.

Критерии приемки:

Обеспечен корректный приоритет операций (AND выполняется раньше OR, скобки меняют приоритет).

Реализована проверка регулярных выражений через оператор LIKE.

[SQL-3] Поддержка агрегатных функций (ТЗ: 12)

Описание: Добавить в SELECT поддержку агрегаций SUM, COUNT, AVG для конкретных столбцов.

Критерии приемки:

Запрос вида SELECT COUNT(id), AVG(age) FROM users; корректно сканирует таблицу (с учетом WHERE) и возвращает ОДНУ строку с итоговыми расчетами.
